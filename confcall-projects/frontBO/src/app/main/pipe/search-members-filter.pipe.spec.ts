import { SearchMembersFilterPipe } from './search-members-filter.pipe';

describe('SearchMembersFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMembersFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
